echo "🔍 Running ESLint and TypeScript type check before push..."

# Run lint but only fail on errors (warnings won't block push)
npm run lint -- --max-warnings=9999 || exit 1

# Run type check
npm run type-check || exit 1

